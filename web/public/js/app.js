'use strict';

// Declare app level module which depends on filters, and services

angular.module('espio', [
    'espio.controllers',
    'espio.filters',
    'espio.services',
    'espio.directives'
]);
